services:
  mongo:
    image: bitnami/mongodb:6.0
    container_name: qtsbook-mongo
    platform: linux/arm64/v8
    ports:
      - "127.0.0.1:27017:27017"
    volumes:
      - mongo_data:/bitnami/mongodb
    env_file: .env
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5

  mongo-express:
    image: mongo-express:latest
    container_name: qtsbook-mongo-express
    ports:
      - "127.0.0.1:8081:8081"
    env_file: .env
    environment:
      ME_CONFIG_MONGODB_SERVER: ${ME_CONFIG_MONGODB_SERVER:-mongo}
    depends_on:
      mongo:
        condition: service_healthy

  app:
    build:
      context: .
    container_name: qtsbook-app
    env_file: .env
    volumes:
      - ./:/app
      - ./reports:/app/reports
      - ./jobdata:/app/.job
    working_dir: /app
    depends_on:
      mongo:
        condition: service_healthy
    ports:
      - "127.0.0.1:8000:8000"
    command: bash -lc "uvicorn app.api.main:app --host 0.0.0.0 --port 8000"

  scheduler:
    build:
      context: .
    container_name: qtsbook-scheduler
    env_file: .env
    volumes:
      - ./:/app
    working_dir: /app
    depends_on:
      mongo:
        condition: service_healthy
    command: bash -lc "python -m scheduler.schedule_daily"

volumes:
  mongo_data:
